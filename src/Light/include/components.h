#ifndef COMPONENTS
#define COMPONENTS
#endif

#include "imgui.h"

namespace Components {
  void resultBox(const char* name, 
      bool selected, ImVec4 foreground, ImVec4 background, float width, float height);
}
